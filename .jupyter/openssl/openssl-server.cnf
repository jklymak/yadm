HOME            = .
RANDFILE        = $ENV::HOME/.rnd

####################################################################
[ req ]
default_bits        = 2048
default_keyfile     = serverkey.pem
distinguished_name  = server_distinguished_name
req_extensions      = server_req_extensions
string_mask         = utf8only

####################################################################
[ server_distinguished_name ]
countryName         = Country Name (2 letter code)
countryName_default     = CA

stateOrProvinceName     = State or Province Name (full name)
stateOrProvinceName_default = British Columbia

localityName            = Locality Name (eg, city)
localityName_default        = Victoria

organizationName            = Organization Name (eg, company)
organizationName_default    = University of Victoria

commonName          = Common Name (e.g. server FQDN or YOUR name)
commonName_default      = Jody Klymak

emailAddress            = Email Address
emailAddress_default        = jklymak@uvic.ca

####################################################################
[ server_req_extensions ]

subjectKeyIdentifier        = hash
basicConstraints        = CA:FALSE
keyUsage            = digitalSignature, keyEncipherment
subjectAltName          = @alternate_names
nsComment           = "OpenSSL Generated Certificate"

####################################################################
[ alternate_names ]

DNS.1       = valdez.seos.uvic.ca
DNS.2       = hornby.seos.uvic.ca
